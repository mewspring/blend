package main

import (
	"errors"

	"github.com/mewmew/blend"
)

// genStruct generates Go structure definitions by parsing the DNA data.
//
// The output is stored in "block_def.go".
func genStruct(b *blend.Blend) (err error) {
	return errors.New("genParse: not yet implemented.")
}
